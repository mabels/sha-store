{
  "name": "sha-storage",
  "version": "1.0.0",
  "description": "write data to pouchdb",
  "bin": "./bin/might-be-use-full",
  "main": "./dist/src/index.js",
  "typings": "dist/src/index.d.ts",
  "files": [
    "dist/src/",
    "bin",
    "src",
    "test"
  ],
  "engines": {
    "node": ">=8.0.0"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "prepack": "npm run clean && npm run lint && npm run test",
    "test": "npm run clean && npm run build && npm run lint && mocha ./dist/test/*-test.js",
    "lint": "tslint --project ."
  },
  "repository": {
    "type": "git",
    "url": "mabels/sha-storage"
  },
  "license": "AFL-2.0",
  "bugs": {
    "url": "https://github.com/mabels/sha-storage/issues"
  },
  "homepage": "https://github.com/mabels/sha-storage#readme",
  "devDependencies": {
    "@types/chai": "4.0.10",
    "@types/mocha": "2.2.45",
    "@types/node": "^9.6.0",
    "@types/uuid": "^3.4.3",
    "chai": ">=4.1.2",
    "mocha": "4.1.0",
    "tslint": ">=5.7.0",
    "typescript": "^2.7.2"
  },
  "dependencies": {
    "pouchdb": "^7.0.0",
    "ts-enum-util": "^3.1.0",
    "uuid": "^3.3.0"
  }
}
